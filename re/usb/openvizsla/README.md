# OpenVizsla USB Traces

This directory contains the usb traces captures by the OpenVizsla USB
protocol capture device.

Due to the sheer size of the raw traces (between 50 MB and 1 GB depending on
the duration of the trace) these are *omitted* from the GIT repo. 

Only the Parsed and Pruned versions are included.

